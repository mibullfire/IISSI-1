/*
 * DO NOT EDIT THIS FILE, it is auto-generated. It will be updated automatically.
 * All changes done to this file will be lost upon re-running the 'silence createapi' command.
 * If you want to create new API methods, define them in a new file.
 *
 * Silence is built and maintained by the DEAL research group at the University of Seville.
 * You can find us at https://deal.us.es
 */

"use strict";

import { BASE_URL, requestOptions } from './common.js';

const aceiteolivaAPI_auto = {

    /** Gets all entries from 'aceiteoliva' */
    getAll: async function() {
        let response = await axios.get(`${BASE_URL}/aceiteoliva`, requestOptions);
        return response.data;
    },

    /** Gets an entry from 'aceiteoliva' by its primary key */
    getById: async function(aceiteID) {
        let response = await axios.get(`${BASE_URL}/aceiteoliva/${aceiteID}`, requestOptions);
        return response.data[0];
    },

    /** Creates a new entry in 'aceiteoliva' */
    create: async function(formData) {
        let response = await axios.post(`${BASE_URL}/aceiteoliva`, formData, requestOptions);
        return response.data;
    },

    /** Updates an existing entry in 'aceiteoliva' by its primary key */
    update: async function(formData, aceiteID) {
        let response = await axios.put(`${BASE_URL}/aceiteoliva/${aceiteID}`, formData, requestOptions);
        return response.data;
    },

    /** Deletes an existing entry in 'aceiteoliva' by its primary key */
    delete: async function(aceiteID) {
        let response = await axios.delete(`${BASE_URL}/aceiteoliva/${aceiteID}`, requestOptions);
        return response.data;
    },
};

export { aceiteolivaAPI_auto };